# JXL Art TODO

## User request (verbatim intent)
Code-golfed JXL art (each .jxl <= 1024 bytes, image >= 1024x1024), driven by cellular automata / MA-tree predictors, verified visually by VLM. No agents for the first pass.

## Tasks
- [x] Check dump dir, tooling (jxl_from_tree, djxl present but dylib broken -> reinstall)
- [x] Fix jxl_from_tree (brew reinstall jpeg-xl) — captured in setup.sh (dump protocol)
- [x] Learn JXL-art tree language from wtf.html + jxl_from_tree.cc
- [x] Dir layout: manifest/concerns/setup.sh at top level, `art/` for everything else
- [x] art/build.py: tree -> .jxl -> .png, prints sizes, fails if > 1024 bytes
- [x] Piece 1: digits (seven-segment 1/0 grid, Rule 30) — 177 B, VLM-checked
- [x] Piece 2: flag (13 stripes, 50 stars) — 261 B, VLM-checked, user approved
- [x] Piece 3: text (3x5 font, 32 chars, Rule 30) — 342 B, generated by art/gen_text_tree.py
- [x] Fix: Rule 30 left-boundary periodicity (Weyl column at x=0)
- [x] Fix: periodic seed row (period 1024 Weyl) — verify visually
- [ ] Flag golf: v3 (223 B) — verify look, promote to art/trees/flag.tree, look for more savings
- [ ] Git commit (user asked) — after the current verification
- [ ] Manifest: keep design sections in sync (flag v3, text generator, lessons)
- [ ] Optional: colour variants / denser digit grid if user wants
