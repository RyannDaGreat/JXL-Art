# JXL Art TODO

## User request (verbatim intent)
Code-golfed JXL art (each .jxl <= 1024 bytes), driven by cellular automata, verified visually by VLM.
Later: flag golf; infinity-masked text (whole characters only), target 300 B; simplify the repetitive code;
then "make it bigger and fit the aspect ratio ... more letters and not square", "space should not be one of the characters".

## Tasks
- [x] Tooling + dump protocol (setup.sh), manifest, concerns, build script
- [x] digits (177 B), flag (222 B after golf)
- [x] Generator rewrite around a tree DSL; text golf (rows-pattern lookup, one counter, RCT grey, Rule 30 on = 1024)
- [x] text_infinity square version 299 B (met the 300 B target)
- [x] Wide 2048x1024 infinity (two groups), no blank glyph -> 315 B; round ellipses + full 32-glyph set -> 440 B; true lemniscate with X crossing -> 471 B; straight-stroke crossing band (de-gloop) -> 524 B
- [x] VLM check of text.png / text_infinity.png
- [x] Manifest + concerns synced
- [x] Commit, `say "Done with Text"`
- [x] Infinity v2: CRT look (scanlines, trail glow, green-amber) -> 602 B (vignette built then removed at user request)
- [x] equations: CFG with matched parens via a depth counter, downward function names -> 534 B (+crt 603 B), verified
- [x] equations v2: dot for multiply, inline SIN/COS/TAN/EXP, syntax colours -> 671 B, verified 60/60
- [x] DSL simplify() pass (libjxl rejects redundant splits)
- [x] equations v2 gap (two per row, 4-cell gap) 675 B; equations_v3 one per line (1024x1024) 658 B; verified
- [x] equations v2: blank row between lines (--row_gap 1) -> 698 B
- [x] equations v2: wider spacing after user feedback (--gap 12 --row_gap 2), 698 B
- [x] equations v2: random-length equations (--random_length), row_gap back to 1
- [x] equations v4: exactly one = per line (--one_equals) and parens sized by depth (--tall_parens)
- [x] equations v4 at 4096x2048 (per-group CA seeds)
- [x] jxl_rs: infinity spelling JXL-RS in Rust colours (--mask lemniscate --logo)
- [x] jxl_rs_crt: amber terminal scanline version (--mask lemniscate --logo --crt)
- [x] jxl_rs_crt: chromatic aberration (rim class 4, per-class G/B deltas)
- [ ] quotes: madlibs grammar of pseudo-profound quotes (--quotes), verify, fit under 1 KB
- [ ] notify when done (say + ntfy)
- [ ] Ideas if wanted: golf digits; colour variants; a frenzy on further size ideas
