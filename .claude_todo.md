# JXL Art TODO

## User request (verbatim intent)
Code-golfed JXL art (each .jxl <= 1024 bytes, image >= 1024x1024), driven by cellular automata / MA-tree predictors, verified visually by VLM. No agents for the first pass.
Later: flag golf; "evolved" text with an infinity-symbol mask (whole characters only); target 300 B for the infinity text; simplify the repetitive code; run `say "Done with Text"` when done.

## Tasks
- [x] Tooling + dump protocol (setup.sh), manifest, concerns, build script
- [x] digits (177 B), flag (222 B after golf), text (32 glyphs)
- [x] Rule 30 fixes: left-boundary periodicity (Weyl column), seed periodicity (mod 1024)
- [x] Generator rewrite around a tree DSL (If/Leaf, chain, runs_tree, dispatch); no pasted blocks
- [x] Text golf: rows-pattern lookup, single cell counter, RCT grey, on = 1024 Rule 30, charset search
- [x] Infinity mask: one-channel polygon field (hexagons) — text_infinity target <= 300 B
- [x] VLM check of final text.png / text_infinity.png (upright letters, ∞ intact)
- [x] Manifest + concerns update (new generator design, cost model, mask options and why)
- [ ] Commit, then `say "Done with Text"`
